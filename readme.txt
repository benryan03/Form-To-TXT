This is my first application using PHP.

index.php is a basic webpage with a text input field and a submit button. When the button is clicked, the text in the field is added to a new line at the end of "file.txt" in the same directory, along with a timestamp.